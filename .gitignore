# Logs (you already have these)
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies / builds
node_modules
**/node_modules/
dist
dist-ssr

# Local machine & editor
*.local
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
Thumbs.db

# Env files (don’t commit secrets)
.env
.env.*
**/.env
**/.env.*
!.env.example        # keep a sample env file in repo

# Test & tooling caches (optional but nice)
coverage/
.eslintcache
.stylelintcache

# Hosting/CI (only if you use them)
.vercel/
.netlify/

# E2E artifacts (only if you use Cypress/Playwright)
cypress/videos/
cypress/screenshots/
playwright-report/

# Notes on the band and the crew
crew-notes/